
import type { ReactNode } from "react";

export default function PrincipalProgressLayout({ children }: { children: ReactNode }) {
  // The AppLayout is provided by the parent principal layout.
  return <>{children}</>;
}
