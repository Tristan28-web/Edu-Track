
import type { ReactNode } from "react";

export default function TeacherSectionsLayout({ children }: { children: ReactNode }) {
  // The AppLayout is provided by the parent teacher layout.
  return <>{children}</>;
}
